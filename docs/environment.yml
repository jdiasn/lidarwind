name: lidarwind

build:
  os: ubuntu-22.04

channels:
  - defaults
  - conda-forge

dependencies:
 - python=3.10
 - numpy
 - xarray
 - pandas
 - matplotlib
 - xarray-datatree
 - nbsphinx
 - sphinx-gallery
 - pip
 - pip:
     - pooch
     - gdown
     - xrft
     - lidarwind
