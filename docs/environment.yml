name: lidarSuit

channels:
  - defaults
  - conda-forge

dependencies:
 - python=3.8
 - numpy
 - xarray
 - pandas
 - matplotlib
 - xarray-datatree
 - pip
 - pip:
     - gdown
     - xrft
     - lidarSuit
